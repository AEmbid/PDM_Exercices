<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MiEstilo" parent="@android:style/TextAppearance">
        <item name="android:textColor">#FF6800</item>
    </style>

    <style name="MiEstilo.Botones">
        <item name="android:padding">50dp</item>
        <item name="android:textSize">8sp</item>
    </style>

    <style name="TemaMio" parent="AppTheme.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="TemaMio2" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@drawable/fondo</item>
    </style>


</resources>
